import { FirstCapDirective } from './first-cap.directive';

describe('FirstCapDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstCapDirective();
    expect(directive).toBeTruthy();
  });
});
